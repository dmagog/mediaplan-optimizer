# Кабинет: что изменено в ветке `feature/pm-cabinet-ux` и что нужно от мозга

Дата: 05.09.2026. Зона: `app/` (кабинет), `tests/test_app.py`, один параметр в `harness/compare.py`.
Основание: три независимых разбора кабинета (инженер, медиапланер, жюри), 80 находок; план и самокритика — в исследовании PM.

## Что изменено в кабинете

**Вёрстка и зависимости.** `[hidden]` больше не перебивается `label{display:flex}`: в постановке B не видно бюджета, в A — целевого объёма. Страница не шире экрана, широкие таблицы скроллятся внутри контейнера с липким первым столбцом. Chart.js лежит локально в `app/static/vendor/`: демо не зависит от Wi-Fi площадки.

**API.** Любая ошибка — JSON `{detail}` по-русски с кодом 4xx, а не 500 text/plain. Прогон только для утверждённого и достижимого плана (409). Утверждение идемпотентно. Проверки на входе: стратегия, сценарий, параметр шока, сила шока > 0, канал шока есть в плане, час шока внутри горизонта, фиксация не больше бюджета, неизвестный пресет, горизонт ≤ 30 дней. Ответ прогона несёт `plan_id`, версию и зёрна; двойник «план без изменений» урезан до трёх полей (985 КБ вместо мегабайт). В `/api/compare` появились `per_run` и число миров в пороге кейса; `hold_plan` теперь реально доходит до стенда.

**Язык.** Идентификаторы каналов, английские имена KPI и запятые в тысячах из строк бэкенда переводятся на фронте картой из каталога. Склонения KPI по числу и падежу. Вердикт демо 2 собирается из полей, а не из строки; ходы отсортированы от дешёвого к дорогому с ценой за единицу.

**Экран плана.** Первая плитка — прогноз с ценой; убраны «вместо рабочего дня в Excel» (исследование это опровергло) и «каналов у потолка 0 из 8» (всегда ноль); столбец цены следующей конверсии третьим и выделен; подпись под таблицей больше не называет неравные цены «признаком оптимума»; VTR со звёздочкой и сноской про допущение; календарь свёрнут с честной подписью про отсутствие недельного профиля; коридор подписан как одна σ; для типа B бюджет назван медианным.

**Экран прогона.** Контролы на языке трафик-менеджера, второстепенные под «дополнительно». Сценарий по умолчанию — пауза канала: единственный, где карточка хода появляется сразу после шока. Плеер идёт по 6 часов и сам останавливается на шоке, детекторе и карточках. Часы без сдвига на единицу. Карточка хода собирается из числовых полей: «Почему / Если сделать / Если не делать», авторство и кнопки от лица человека («Перенести», «Оставить как есть»). Сводка: приоритет пожар → наблюдаем → ждёт решения → норма; карточки старше суток не висят в совете. Лента объединяет шоки, детектор и ходы.

**Итог.** «План выполнен» только когда он выполнен; плитка порога кейса для нас и для плана без изменений; хронология с ходами; в сравнении на 20 мирах — миров в пороге и победы по расходу, а не только по KPI, плюс честный абзац: по MAPE по часам план без изменений часто ближе, потому что тратит ровно по плану.

**Тесты.** `tests/test_app.py`: демо 1 отдаёт всё по кейсу, демо 2 отказывает с тремя ходами, прогон только после утверждения, воспроизводимость по зёрнам, русские 422/404.

## Запросы к MLE #1 (в `brain/` не трогал)

Все шесть подтверждены живым API на плане демо 1, зерно 1.

1. **Карточка при срабатывании детектора.** На `ctr_drop` и `cpm_spike` детектор срабатывает на 270-м часе, статус «пожар» 271–294, а ближайшие карточки — на 48-м и 444-м. Момент демо 3 «статус краснеет и всплывает карточка» не наступает. Нужно: в час детекции эмитить `Proposal` с донором = сломанный канал и суммой = разница перерешённого остатка.
2. **Первая карточка на 12-м часе в спокойном мире.** 101 719 ₽ из SMS в programmatic, на 18-м ещё 86 702, на 24-м — 65 291: половина бюджета SMS уходит за сутки без шока, опровергая только что утверждённый план. Вероятная причина в `_adjusted_curves`: `observed_ecpm` сравнивается с `catalog.ecpm_mid`, а не с ценой из кривой на плановом уровне.
3. **Лимит полномочий не ограничивает.** При ожидающей карточке на 101 719 ₽ система штатно переносит 86 702, 65 291 и 57 585 ₽ — каждый ниже лимита, сумма втрое выше. Нужен лимит по скользящим суткам либо заморозить долю канала-донора, пока карточка ждёт.
4. **Цена бездействия** делится на остаток плана: «недоберём 57 %» за 12 часов до конца при итоговом отклонении 6,4 %. Делить на весь план и отдавать абсолют в `inaction_kpi_shortfall_abs`.
5. **Детектор слеп к падению CR**, а KPI кейса — конверсии: на `cvr_drop` детектор молчит, статус «в норме» весь шок.
6. **VTR 0,35** литералом в `planner.py:246` вместо чтения `config/assumptions.yaml`.

## Круг третий: после встречи с кейсодателем

Его приоритеты: два интерфейса, произвольный набор каналов, распределение отклонений на многих мирах, «осторожный план», красивый и простой дизайн. Что сделано в кабинете (ветка та же, `app/` и тесты):

- **Два уровня одним переключателем.** «Кабинет» — для рекламодателя: бриф, план, ведение с графиками и карточками, итог. «Стенд» — плюс сценарии рынка, свои шоки, стратегии, зерно, стресс-тест, сравнение на 20 мирах, таблица часа, вердикт с зёрнами. Технические элементы помечены классом `tech`, уровень запоминается в браузере. Полоса этапов «Бриф · План · Ведение · Итог» липкая и показывает статус каждого этапа.
- **Любой набор каналов.** Восемь чипов в брифе, пресеты подсвечивают набор; бэкенд получает `channel_ids`. Вариант «добавить каналы» из демо 2 меняет чипы.
- **Сценарии карточками, свои шоки чипами.** Карточка сценария описывает событие словами (канал, что ломается, сила, с какого дня и на сколько; события за горизонтом плана помечены). Свои шоки — до трёх, форма прячется за «+ Свой шок». Кнопка «Запустить кампанию» подписана тем, что случится на рынке.
- **Обещание клиенту.** «По прогнозу» — медиана с разбросом; «с запасом» — нижняя граница разброса, в постановке «нужен бюджет» — бюджет с резервом (бюджет считаем под цель, увеличенную на ширину коридора, а обещаем исходную). Итог сравнивает факт с обещанием, а не только с планом.
- **Гистограмма отклонений по мирам** в сравнении стратегий: корзины по 5 %, порог кейса линией, наше ведение против плана без изменений. Сотни миров — офлайн-стенд, см. запрос ниже.
- **Дизайн.** Одна высота и одно скругление у всех контролов и кнопок (нативный select перерисован, иначе Safari его не выравнивает), три вида кнопок, номера этапов у разделов, плитки крупнее, лишние подписи убраны из «Кабинета».

Запросы к коллегам по приоритетам кейсодателя (не наша зона):

7. **Стенд на сотнях миров** (MLE #1): `scripts/run_demos.py --seeds 100` и распределение отклонений (гистограмма или боксплот) в `results/report.md` — слайд «почему решению можно верить». Кабинет умеет до 30 миров синхронно, дальше — офлайн.
8. **Слайд «почему миру можно верить»** (MLE #2): какие свойства мира проверены тестами и бенчмарками, что упрощено.
10. **Пауза канала не считается сломом** (MLE #1): при `channel_pause` лента пишет «канал не отдаёт показы 3 часа подряд» и идёт перенос, но `detection_hours` пуст и статус часа остаётся «в норме»; кабинет обещает «при сломе канала — «пожар»». Заносить паузу в `detection_hours` и поднимать статус.
11. **Текст варианта «Снизить цель до N в срок»** (`brain/planner/planner.py`): «в срок» читается как «вовремя»; лучше «при тех же каналах и сроке».
9. **Пересечения аудиторий** (задача со звёздочкой, MLE #2 и #1): матрица пересечений в каталоге и учёт в охвате; кабинет покажет её отдельной таблицей, если появится в `/api/meta`.

## Круг четвёртый: дизайн Modernist

Кабинет пересобран по макету из Claude Design (проект «Платформа оптимизации рекламных кампаний», файл `MediaPlan Optimizer.dc.html`, дизайн-система Modernist): четыре экрана по одному за раз с шагами в липкой шапке, переключатель «Просто / Подробно» вместо «Стенд / Кабинет», кикеры разделов, плитки-статы с верхней линейкой, таблица с полосой доли бюджета и переключателем «Кратко / Все метрики» (агентству — все метрики сразу), пульт кампании с сегментом шага, сводка с отдельным блоком «Что делать», крупная фраза итога. Токены системы — в `:root` файла `index.html`; шрифты Archivo (латиница, цифры) и Golos Text (кириллица — у Archivo её нет) лежат в `app/static/vendor/fonts`. Графики в цветах системы: план — чернила пунктиром, факт — акцент, план без изменений — нейтраль, задетый шоком канал в стеке — акцентом. Логика и API не менялись; стенды переехали в блок под кампанией.

## Круг пятый: пользовательские сценарии и аудит дизайна

Три персоны (медиапланер, трафик-менеджер, рекламодатель с руководителем) прошли сценарии С1–С6 в живом кабинете через Playwright, затем два взгляда на дизайн — продуктовый дизайнер и человек без подготовки — разобрали каждый элемент. Внесено:

- **Бриф.** Правила ведения (лимит переноса, перевыполнение, потолок цены конверсии) — отдельным свёрнутым блоком, потолок только для конверсий; деньги с разрядами и подсказкой «= 1,2 млн ₽»; подписи по-человечески («конверсии — заявки и покупки», «Что записываем как обещание»); примеры — ссылками под главной кнопкой; состояние «Считаем…» в самой кнопке.
- **Шапка.** Статус шага видимой строкой под названием, недоступные шаги серым без прозрачности; «Вид: Просто / Подробно» вместо «Агентство / Рекламодатель» — сегмент читался как чужой вход и как главная кнопка. Один красный на экран: выбранные теги, сегменты и переключатель — чернилами с галочкой; главные кнопки — accent-600 с белым (контраст 4,75:1); muted и заголовки столбцов — neutral-700 (5,8:1).
- **План.** Свежий расчёт не считается утверждённым из чужой сессии; после утверждения остаёмся на плане (в простом виде «Утвердить и запустить кампанию» ведёт сразу к прогону); одна кнопка «Утвердить»; плитки → «что если» / стресс-тест → таблица; ключевой столбец четвёртым в обоих режимах, «Выкуп» рядом; в простом виде таблица показывает результат и цену; полосы доли от лидера; тень и подпись у широких таблиц; календарь над таблицей (подробный вид), в простом — строка «около N ₽ в день»; второй график — расход по дням и группам каналов вместо прямой; экран отказа: «Цель недостижима — есть три варианта», рекомендуемый вариант главной кнопкой, «Изменить бриф»; после варианта — плашка «Применён вариант N».
- **Кампания.** Карточка «Что запускаем» до запуска; липкий пульт с часами и статусом; «▶ Смотреть», «Скорость:», «день 11 из 21»; причина остановки в сводке; плитка «Ждёт вашего решения / следующая остановка»; карточка решения и плашка «Решение принято» под сводкой с прокруткой к ней и советом «перенести»; ссылки из «Что делать» на карточку и ленту; короткая лента у совета; таблица часа «Кратко / Все столбцы»; метка события на графике — только когда прогон до неё дошёл; стек долей по группам каналов, задетый шоком канал акцентом; статус «сбой канала» вместо «пожар»; кнопка «К итогу для рекламодателя» у часов и в совете.
- **Итог.** Главная фраза с ценой единицы, округлёнными суммами и вердиктом; «Что дало ведение» деньгами; цена по каналам, строка «Итого», вывод «лучше всего / хуже всего»; исход нерешённых карточек в хронологии; «Сохранить итог в PDF» (печать); стенды переехали к итогу, сравнение с выводом одной фразой и нашим ведением первой строкой, стресс-тест от худшего шока.

Не наша зона, передано (запросы 10–12): пауза канала не поднимает статус; «≈ 33 % недоберём» в карточке против итоговых −5 %; текст варианта «в срок».

### Повторный прогон персон

Те же три персоны прошли сценарии заново на обновлённом кабинете. Шагов с сильным трением (2–3 из 3): медиапланер 7 → 4, трафик-менеджер 6 → 6, рекламодатель — 5 при большем числе шагов. Новое в отчётах — в основном последствия самих правок, закрыты тем же днём: статусы шагов растягивали липкую шапку, и она накрывала пульт (теперь статус не шире 150 px, а пульт пересчитывает отступ при каждом изменении шапки); подпись «Вид:» отрывалась от сегмента (одна группа); подсказка под полем бюджета ломала линию полей (подсказка в подписи поля); раскрытые правила ведения уводили кнопку расчёта под сгиб (правила ниже кнопки, обещание внутри правил); календарь открывался над таблицей (теперь под ней, с прокруткой к нему); переключатель «Кратко / Все метрики» стал сегментом, «Частота» убрана, подсказка о прокрутке — над таблицей; таблица часа во всю ширину в порядке кейса; ждущая карточка не дублируется в списке переносов; «Запустить заново» спрятан под технику; итог: «в допуске» вместо «выполнено» при недоборе, лучший и худший канал по цене единицы, цена дополнительной конверсии у «Что дало ведение», стенды сразу под таблицей по каналам, счётчик секунд у стендов и стресс-теста, вывод сравнения начинается с выигрышного критерия; пересчёт после утверждения спрашивает подтверждение; на 1440+ рабочая ширина 1360.

Осталось за рамками (нужны решения команды или мозга): откат и правка суммы переноса на карточке, лимит полномочий на экранах плана и кампании, дата старта кампании и SMS-пакеты в брифе, «наблюдаем / сбой канала» не появляются при паузе канала (запрос 10), стенд усиления не находит границу на ×1…×4 (расширить диапазон), выгрузка таблиц в CSV.

12. **Цена бездействия в карточке** (MLE #1): «недоберём ≈ 33 % к концу» при итоговом отклонении 5 % — делить на весь план (см. запрос 4) и не показывать проценты в последние сутки.

## Круг шестой: остаток по аудитам и одно правило для кнопок

Что смущало PM: главное действие экрана гуляло — слева внизу на брифе, справа вверху на плане, у пульта на кампании, внизу на итоге. Правило теперь одно: **главное действие экрана — справа в шапке экрана; на длинном экране оно повторяется внизу в том же виде**. Бриф: «Рассчитать план» вверху справа и внизу формы (примеры — рядом с нижней). План: «Утвердить план» вверху и внизу, после утверждения оба становятся «Перейти к кампании». Кампания: «Запустить кампанию» вверху справа; после прогона там же появляется «К итогу для рекламодателя», у часов остаётся короткое «К итогу →»; кнопки пульта и карточек — не действие экрана. Итог: «Сохранить итог в PDF» вверху и внизу. Второстепенные кнопки («Что если», стресс-тест, CSV, переключатели) стоят рядом с тем, к чему относятся, и всегда вторичного вида.

Закрыто из «за рамками»: откат своего одобрения — кнопка «Отменить мой перенос» на карточке, бэкенд перепрогоняет кампанию без этого часа (`decision: undo`, тест); лимит на один перенос показан на плане строкой под плитками; дата старта в брифе — даты в подписи плеера, в календаре, на графике расхода по дням и в «Что запускаем»; статус «сбой канала» показывается по событию рынка и детектору, даже если разброс плана в норме (подсказка на плашке объясняет, откуда статус); стенд усиления идёт до ×8 и ×0,1 и честно пишет, если границы нет; «Скачать CSV» у таблицы плана и таблицы итога; подсказки к терминам на заголовках столбцов и плитке разброса; в простом виде «CPA» везде читается как «цена конверсии»; мелкие переносы (меньше 0,5 % бюджета) спрятаны в ленте за ссылкой; на телефоне шапка не липкая.

Остаётся у мозга (`brain/`): правка суммы переноса в карточке и откат автоматических переносов (исполнитель принимает только `approved_hours`), SMS-пакеты в брифе, статусы «наблюдаем / пожар» от самого исполнителя при паузе канала.

## Круг шестой: ручной аудит экранов, откат переноса, слияние с мозгом

- **Ручной аудит** всех экранов в обоих видах (агенты-аудиторы зависали на длинных снимках, поэтому смотрел сам): одна главная кнопка на брифе и на итоге, «Стратегия: наше ведение» вместо обрывка в вердикте стенда, подписи дней на графиках каждые трое суток, хеш плана убран из кикера в подробности, короткий кикер кампании, «конверсий на рубль» вместо KPI в карточках, «до 35 451 клика при тех же каналах и сроке» в варианте отказа, три технические плитки итога в одну.
- **Откат переноса** (С5): `POST /api/run/{id}/decide` принимает `decision: "undo"` — перепрогон без этого часа в `approved_hours`, эффект считается так же, как при одобрении; кнопка «Отменить мой перенос» на карточке, сделанной по решению человека. Переносы автоматики откатить нельзя — это не решение человека (в исполнителе нет такого входа).
- **Решение основателя:** CPA, CPC, KPI — профессиональные термины, остаются как есть в обоих видах; расшифровка — подсказкой при наведении.
- **Слияние с `develop`** (мозг после двух ревью, стенд на 100 мирах): конфликты в README (результаты стенда и описание резерва — версия MLE #1, с оговоркой, что запас обещания уже задаётся в кабинете) и `harness/compare.py` (версия MLE #1 с `hold_plan` и `reserve_balance`). На новом исполнителе первые сутки идут без перерешения, карточки — раз в сутки на канал, и при лимите 100 000 ₽ ждущих карточек в демо 1 нет; пример и тест переведены на лимит 50 000 ₽ — карточки ждут решения в день 1, 24:00 и в час паузы programmatic (день 11, 03:00), детектор ловит programmatic в тот же час. Сценарий показа обновлён.
- **Логотип и фавиконка** в стиле Modernist: чернильный квадрат без скруглений, бумажная прорезь-коридор, красная линия факта. **Safari:** нативные select и поле даты не слушались высоты — свой вид со стрелкой, все контролы ровно 36 px.

## Круг седьмой: третий прогон персон и правки по нему

Третий прогон (на версии до слияния мозга): медиапланер 15 (было 18), трафик-менеджер 20 (21), рекламодатель 29 — у него сервер был занят тремя персонами и стендами одновременно (сравнение считалось 2,5 минуты), и вскрылась настоящая дыра: клик по плееру во время пересчёта решения молча терял решение. Внесено:

- **Плеер останавливается только там, где нужен человек или случился шок:** карточки, ждущие решения, шоки, детектор, события рынка; переносы автоматики ниже лимита идут в ленту без остановки. При остановке — статус «нужно решение», баннер «Плеер остановился…» с кнопкой «Продолжить ▶», карточка прокручивается в кадр.
- **Пересчёт решения блокирует плеер и карточки** на 5–10 секунд с подписью; стенды блокируют друг друга, таймаут объясняет, что считается другой стенд.
- **Бриф:** правила ведения и обещание — над кнопкой расчёта (свёрнуты), главная кнопка этапа снова вверху справа (внизу — второстепенная копия у примеров), пресеты — строка «Быстрый выбор набора» текстовыми кнопками с подчёркиванием, каналы — теги с галочками; подпись «с запасом» меняется по режиму; на плане «Кратко» по умолчанию в обоих видах, связка «обещаем 2 271, при обычном разбросе не меньше 1 496», причина цены запаса («дешёвые каналы выкуплены: … на 100 %»), CSV в единицах экрана; таблица часа — сегмент «Кратко | Все столбцы»; в «Что запускаем» свой шок словами; объяснение, почему дорогой канал получает деньги.

## Круг восьмой: два кабинета — заказчик и менеджер

Кейсодатель просил два интерфейса; до этого они были «видами» одной страницы («Просто / Подробно»), и роль по экрану не считывалась. Теперь переключатель в шапке называется «Кабинет: Заказчик / Менеджер», имя кабинета стоит первым словом кикера на каждом экране, у вкладки браузера тоже, адрес `?role=customer|manager` (старые `?level=simple|tech` работают). Разделение по смыслу, а не только по словам:

- **Заказчик** видит бриф, план с обещанием, кампанию по часам и итог. Карточки переносов выше лимита он видит с подписью «Решает менеджер» и без кнопок; плеер на них не останавливается (только на шоках, событиях и детекторе); в ленте и на итоге — «менеджер перенёс», «ждали решения менеджера».
- **Менеджер** — всё, что раньше называлось «Подробно», плюс решения по карточкам и их откат.
- Снимки и гифка в `docs/figures/cabinet/` пересняты: `customer-*`, `manager-*`, `player.gif` (кабинет менеджера).

## Круг девятый: география, ручные бегунки, версия и подвал

Просьбы основателя от 07.09. Что сделано в кабинете:

- **Логотип перерисован.** Прежняя «Прорезь» в 24 px превращалась в кашу. Новый знак — три канала столбцами и красный блок бюджета, перенесённого в лучший: три фигуры и разрыв, читается и в шапке, и во вкладке. Фавиконка — тот же знак на чернильном квадрате.
- **Номер версии** рядом с логотипом и в подвале, приходит из `/api/meta`; `tests/test_app.py` сверяет его с `pyproject.toml`, чтобы на показе не разъехались.
- **География.** Округа выбираются тегами или кликом по карте России (`app/static/russia.svg`, Natural Earth, проекция Альберса, 83 субъекта в восьми группах). Выбор пересчитывает ёмкость каналов на долю аудитории округов, и тот же каталог уходит в план и в прогон (`app/geo.py`). Контракт брифа не тронут: `brain` и `world` про регионы по-прежнему не знают.
- **География в примерах.** Третья кнопка-пример «один округ: 400 тыс. ₽ на Центральный»; в демо-скрипте появилась секция «Демо 2б». На экране отказа добавлена подсказка «расширить географию» с текущей долей аудитории.
- **Ручные бегунки.** На экране плана канал фиксируется бегунком (`locked`), остальное планировщик раскладывает заново; если канал упёрся в ёмкость, кабинет объясняет это словами. Бюджет по округам двигается такими же бегунками, «Поделить по населению» возвращает расчёт.
- **Подвал** с версией, происхождением чисел, границей прототипа, командой и ссылкой на репозиторий.

Запрос владельцу мира (MLE #2), не наша зона: региональная модель — своя ёмкость и своя цена контакта по округам, пересечение аудитории соседних округов, разное проникновение интернета. Сейчас кабинет честно пересчитывает только ёмкость на долю населения и об этом прямо пишет; деление бюджета между округами остаётся плановой величиной.

## Круг десятый: сегмент, правила отдельным окном, порог молчания

- **Таргетинг коллег соединён с нашей географией.** Оси мира (возраст, пол, тип населённого пункта) выведены в бриф; сегмент и округа перемножаются как независимые, и кабинет это проговаривает. Несочетаемые условия ловятся по данным о городах: «столицы» без ЦФО и СЗФО — пустое условие. Каталог считается один раз на пару «сегмент + доля округов» (`_world` поверх `_target_context` коллег).
- **Карта поднята** к началу блока географии: раньше она висела ниже тегов и блок читался пустым.
- **Правила ведения — в отдельном окне** вместо раскрывающегося блока: обещание, лимит переноса и потолок цены, пороги ожидания решения, режим бюджета. На экране брифа осталась строка-сводка текущих правил, в шапке рядом с переключателем кабинета — шестерёнка (иконка gear из Bootstrap Icons, MIT, вставлена разметкой: демо не зависит от сети). В окне сказано, что действует сразу, а что со следующего расчёта плана.
- **Что делать, если менеджер молчит.** Два порога: через N часов система применяет предложение сама (в ленте «система применила сама», откат кнопкой), через M часов кампания встаёт и плеер не идёт дальше, пока решение не примут. M должно быть больше N, иначе окно не даёт сохранить. Ноль выключает правило.
- **Подвал**: убрано упоминание кейса и хакатона, ссылка ведёт на наш репозиторий.

Правка бюджета прямо в строке таблицы (по вопросу основателя, не ломает ли это концепцию): ползунок в строке отвергли — таблица едет вбок в режиме «Все метрики», её выгружают в CSV и печатают, а перетаскивание конфликтует с горизонтальной прокруткой и умирает при перерисовке. Вместо него сумма правится в ячейке и шаг ±10 % кнопками; действие то же, что и у бегунков ниже, — фиксация канала.

Правки по ревью двумя взглядами: остановка держится на всех путях (плеер, «До следующего события», «К концу кампании», бегунок — через один потолок часа `stopCap`), автоматическое решение идёт раньше остановки, откат выключает правило для этой карточки, Escape не выпускает из окна с запрещённой парой порогов, «Что запускаем» описывает утверждённый план, а не текущее состояние формы, баннер остановки не затирается плеером.

Запрос владельцу мозга (MLE #1), не наша зона: пороги ожидания сейчас — правило кабинета поверх готового прогона (он применяет решение тем же вызовом `/decide`, что и человек). В продукте это правило исполнителя: «не решено за N часов — применяю сам», «не решено за M — останавливаю кампанию и не трачу бюджет дальше». Второе особенно важно: сейчас остановка видна только в плеере, а в модели кампания продолжает тратить деньги.

## Круг одиннадцатый: сверка с постановкой второго курса

Проверили репозиторий против задания второго потока (задача №5, дедлайн 27.09) двадцатью четырьмя агентами по группам требований; четырнадцать пробелов подтвердились, пять замечаний сняли при перепроверке. Первый блок правок:

- **Итог по плану целиком.** Постановка требует те же метрики, что по каналам, но и по медиаплану в целом; их не было ни в ответе API, ни в таблице. Добавлены `totals` в ответе плана и строка «Итого» в таблице и выгрузке: объёмы суммой, качество и цены — от объёмов (взвешенно, а не средним арифметическим долей). У VTR рядом идёт доля показов, на которой он посчитан.
- **Разрез по дням и неделям.** В ответе появились ряды `series.days` и `series.weeks` (бюджет за период, накопленный бюджет и накопленный результат), на экране плана — блок с переключателем «По дням / По неделям». Раньше был только почасовой ряд, и дневные точки приходилось выбирать руками.
- **Понятная ошибка на бюджет.** Нулевой и отрицательный бюджет отклонялись сообщением про сумму фиксаций каналов. Теперь своё сообщение и такое же для целевого объёма.
- **Тесты агрегации метрик.** У `harness/metrics.py` не было ни одного прямого теста; добавлен `tests/test_metrics.py` на MAPE, WAPE, конечное отклонение, unsmoothness и коэффициент вариации, включая вырожденные случаи.
- Побочно: режим «Все метрики» протекал в кабинет заказчика при переключении, теперь таблица там всегда короткая.

Осталось из сверки: оптимизация бюджета по дням и неделям, ускорение диагноза недостижимости, ход на потолок цены, диагноз для постановки типа A — это зона планировщика (закрыто в круге тринадцатом); из артефактов сдачи нет demo-notebook, отчёта в `/report` и скринкаста (закрыто в круге двенадцатом).

## Круг двенадцатый: артефакты сдачи

По списку задания второго курса не хватало трёх вещей, теперь они есть:

- **Ноутбук со сквозным сценарием** — `notebooks/demo.ipynb`: каталог, план типа A, метрики по плану целиком, недостижимая цель типа B с тремя ходами, кампания по часам с шоком и решением человека, итог, тот же путь через API. Собирается скриптом `scripts/build_demo_notebook.py`, который выполняет ячейки и кладёт вывод в файл: jupyter в зависимости проекта не тянем.
- **Итоговый отчёт** — `report/report.html`: постановка, что делает сервис, модель и решатель, архитектура, эксперименты, границы, выводы. Собирается из README, `docs/report.md` и `results/report.md` скриптом `scripts/build_report.py`, картинки вшиты в файл.
- **Скринкаст** — `docs/screencast.mp4`, 2,6 минуты: бриф с сегментом и картой, правила ведения, план со строкой «Итого» и разрезом по неделям, ручная правка, кампания по часам с карточкой решения и шоком, итог, затем недостижимая цель с тремя ходами. Запись делает `scripts/record_screencast.py` через Playwright.

## Круг тринадцатый: зона планировщика

Четыре пункта сверки, которые оставались за планировщиком, закрыты.

- **Бюджет по дням недели.** Разбор показал, что дело было не в планировщике: в мире множитель выходных сокращался при нормировке профиля внутри суток, и разницы дней не существовало. Мир теперь нормирует профиль по неделе (`weekend_volume_ratio` вынесен в допущения), планировщик считает вес каждого дня по ретро и раскладывает бюджет пропорционально спросу (усадку веса к средним суткам замерили и отвергли — против истинных весов мира она не выигрывает). Конверсий это не прибавляет (для вогнутой кривой «пропорционально спросу» — точный оптимум, разница с равномерной раскладкой 0,0 %), зато календарь и часовые лимиты плана совпадают с тем, как кампания тратит на самом деле.
- **Диагноз недостижимости за секунды.** Перебор горизонта день за днём (7–32 с) заменён двоичным поиском: максимум достижимого растёт со сроком, поэтому хватает логарифма от диапазона. Отказ считается за 0,2–0,9 с.
- **Потолок цены назван ограничением.** Если без потолка цель достижима, отказ теперь называет `economics`, а первым ходом идёт «поднять потолок средней цены до N ₽» с посчитанным бюджетом; ход применяется той же кнопкой, что и остальные.
- **Диагноз для типа A.** Бриф с бюджетом всегда даёт план, поэтому объяснения не было. Теперь план говорит, сколько бюджета размещено, что именно не пустило остальное (потолок цены — с суммой и прогнозом без него; ёмкость — со сроком, за который бюджет разместился бы целиком), и кабинет показывает это плашкой на плане, а не только в раскрытом объяснении.

## Круг четырнадцатый: правки по ревью зоны планировщика

Шесть направлений ревью с тройной проверкой каждой находки дали 43 подтверждённых
дефекта; разбор — в журнале решений, запись 37. Кабинета касается вот что:

- Заголовок экрана отказа считает ходы, а не обещает «три варианта»: при связывающем потолке цены их два.
- Плашка «бюджет размещён не весь» поднимается только на значимом недоборе и только когда планировщик назвал причину; раньше её поднимало сто рублей округления.
- Ход «поднять потолок средней цены» больше не теряется, если после расчёта в брифе сменили KPI: кабинет говорит об этом прямо, а не применяет ход вхолостую.
- Ход «снизить цель» с нулевой целью исчез (он отдавал 422 и затирал цель клиента), «Упирается в» читается в правильном падеже, цены ниже десяти рублей печатаются с копейками.
- Три подписи про «бюджет разложен по дням равномерно» переписаны: раскладка идёт по спросу дня.
- Заказчик видит первую причину недоразмещения, подробности про ёмкость и сроки — у менеджера.

## Что осталось за рамками

- Стресс-тест 42 с и сравнение 69 с идут синхронно; вместо распараллеливания — честная оценка времени в интерфейсе и таймаут три минуты.
- Кнопки «шок сейчас» во время воспроизведения нет: прогон считается целиком заранее, кнопка потребует перепрогона с середины.
- Откат применённого хода: одобрение уже реализовано перепрогоном на тех же зёрнах, откат — тот же перепрогон без хода; нужна только кнопка, механизм есть.
