# Кабинет: замечания рецензентов, не внесённые в код

Дата: 05.09.2026, вечер. Ветка `feature/pm-cabinet-ux`, файл `app/static/index.html` (после коммита `1b5cc07`), местами `app/main.py`, `README.md`, `docs/cabinet_changes.md`.

Два независимых разбора после того, как кабинет переписали: редактор (язык интерфейса) и инженер (логика, состояния, контракт с API).

**Статус 06.09.** Внесено в ветку почти всё (коммит «кабинет: правки двух рецензентов»). Не внесено и остаётся на потом: переписать README «Три демонстрации» короткими предложениями по экранам (редактор, документы); запретить свой шок по каналу с нулевым бюджетом (инженер, п. 12 — дубли уже запрещены). Спорные пункты решены так, как записано ниже в «Решениях PM». Таблицы ниже оставлены как были — по ним удобно проверять, что и почему поменялось.

**Проверка после правок (три независимых ревьюера: инженер, редактор, репетиция демо по сценарию).** Найдено и внесено тем же днём: кнопки стендов снимают блокировку в `finally`; «Последний перенос» не называет ждущий перенос сделанным; «Переносов автоматики» считает только автоматические; карточка текущего часа стоит первой, ждущая — второй; хронология итога в причинном порядке (шок → событие → перенос); сводка называет события рынка за сутки, даже когда детектор молчит; «со дня» везде; множители через запятую в стенде усиления и стресс-тесте; тысячи из бэкенда неразрывным пробелом; `?level=simple|tech` в адресе важнее запомненного уровня; блок «Дополнительно» в «Стенде» раскрыт; заголовок гистограммы оговаривает, что «в пороге» учитывает и расход; знак «+» у положительных Δ; README «Три демонстрации» переписан по экранам; `main.py`: «варианты», «перенос», поля брифа названы как в интерфейсе. Не наша зона, передали в `docs/cabinet_changes.md` (запросы 10–11): пауза канала не поднимает статус и не попадает в `detection_hours`; текст варианта «в срок».

## Как вносить

- Тексты интерфейса — по правилам `/ru-text` и `/humanizer-ru`: живой русский без калек, тире «—», «ёлочки», неразрывный пробел перед ₽ и % (в JS-строках ` `, в HTML `&nbsp;`; функции `rub()` и `pct()` уже ставят его сами). Обычные пробелы внутри JS-выражений (`% 100`, `% 24`) не трогать — это операторы.
- Технические элементы, которые не должен видеть рекламодатель, помечаем классом `tech`: в уровне «Кабинет» их скрывает правило `body[data-level=simple] .tech{display:none}`.
- После правок: `set -o pipefail && .venv/bin/ruff check app tests && .venv/bin/python -m pytest -q tests/test_app.py`. Проверка в браузере: пример «1,2 млн ₽ на 21 день» → «Утвердить план» → «Запустить кампанию» → «К итогу», в обоих уровнях.
- Коммиты в ту же ветку, без соавторов-ботов в сообщениях.
- Строки в таблицах — по состоянию файла на момент разбора; после первых правок ищите по тексту «сейчас», не по номеру.

## Часть 1. Редактор: язык интерфейса

Найдено 45 мест. Из них три грамматические ошибки в коде, 24 места без неразрывного пробела, пять имён одной и той же стратегии.

### Решения PM по спорным пунктам

- Кнопку «Что если бюджет −10 % / +10 %» переименовать в «Что если бюджет ±10 %», а не в «Бюджет ±10 %»: без «что если» кнопка не объясняет, что делает.
- Плитка «Бюджет» в постановке B: «по медиане: цели хватит в половине случаев», не «средний» — бюджет медианный, слово «средний» соврёт.
- «Цена бездействия» в `docs/cabinet_changes.md` остаётся как имя поля для коллег; в интерфейсе уже «Если не делать».
- Всё остальное из таблиц ниже — принять.

### Кабинет (видно на демо)

| Где | Сейчас | Предложение | Почему |
|---|---|---|---|
| `renderSummary`, `returnedNote` | `недобор по ${K.many} …` → «недобор по конверсий 12 %» | `недобор по ${K.dat}` → «по конверсиям» | Ошибка падежа: после «по» дательный |
| `renderPlan`, вердикт демо 2 | «С выбранными 1 каналом…»; «Упирается в: ёмкость каналов.» | «С одним каналом / 4 каналами за 14 дней максимум …»; «Упирается в ёмкость каналов.» | «выбранными» не согласуется при n = 1; двоеточие после «упирается в» угловато |
| `renderPlan`, плитка «Прогноз» | «CPA 517 ₽ · это и обещаем клиенту» | «CPA 517 ₽ · обещание — по прогнозу» | Разговорная калька; смысл дублирует выбор «Обещание» |
| бриф (label `#promise`), `renderPlan` плитка, `renderSummary` вердикт и плитка | «Обещание клиенту», «Обещаем клиенту», «Для рекламодателя.» | «Обещание», «Обещаем», «Коротко.» | Читатель «Кабинета» и есть рекламодатель; «клиент» в третьем лице путает голос. «Обещание клиенту» оставить в README и docs |
| бриф, подсказка `#promise` | «с запасом — нижняя граница разброса» | «с запасом — обещаем по нижней границе прогноза» | «Разброс» без объекта непонятен человеку без техники |
| `renderPlan`, плитка «Разброс прогноза» | «±34 %: одна σ из ширины диапазонов каталога» | «±34 % — обычный разброс между кампаниями`<span class="tech"> · одна σ из ширины диапазонов каталога</span>`» | σ и «каталог» — жаргон симулятора |
| `renderPlan`, заголовок графика | «Плановая траектория, конверсий: коридор ±34 % (1σ)» | «План по конверсиям, разброс ±34 %» (`'План по ' + K.dat + …`) | «(1σ)» — жаргон; «траектория, конверсий» — запятая рвёт фразу |
| `renderPlan`, плитка «Бюджет» (тип B) | «медианный: цель выполняется в половине миров» | «по медиане: цели хватит в половине случаев» | «Мир» — жаргон |
| тултип переключателя `.seg`, `showHour` плитка, `decideProposal` баннер | «проверки на многих мирах»; «тот же мир, план буквально»; «Тот же мир, те же случайные числа» | «проверка на многих прогонах»; «тот же рынок, без переносов»; «Тот же рынок и та же случайность — разница только от вашего решения» | «Мир» и «зёрна» в «Кабинете» — жаргон; тултип виден всем |
| `prepareRun` h3, `showHour` плитка «Допуск коридора», пустая лента, `digest` совет и строка «Детектор:», `renderSummary` h3 | «Лента: шоки, детектор, ходы»; «после детектора — «пожар»»; «детектор молчит»; «детектор увидел слом канала…»; «Детектор: SMS (…)»; «Хронология: шоки, детектор, ходы» | «Что происходило»; «при сломе канала — «пожар»»; «пока тихо: ни событий, ни переносов»; «канал сломался, автоматика заново делит остаток — смотрите карточку»; «Слом канала: SMS (…)»; «Хронология событий» | В «Кабинете» человек видит следствие (слом), а не механизм (детектор). В «Стенде» (вердикт, стресс-тест) «детектор» остаётся |
| слово «ход» = перенос денег: label `#auto_limit`, h2 раздела 3, `prepareRun` h3, `cardText` ifNot, `digest` «Последний ход», `decideProposal` баннеры, `renderSummary` плитка и пустая хронология | «Лимит автоматики за один ход», «план, факт, ходы», «Предложения и ходы автоматики», «ход экономит бюджет», «Последний ход», «Ход день 12, 06:00 сделан», «Ходов автоматики», «шоков и ходов не было» | «перенос» везде: «Лимит на один перенос, ₽», «план, факт, переносы», «Переносы бюджета», «перенос экономит бюджет», «Последний перенос», «Переносов автоматики», «событий и переносов не было» | Интерфейс уже говорит «Перенести» и «переносов пока не было»; «ход» — шахматная метафора, к тому же занята демо 2 |
| слово «ход» = предложение планировщика: `stage('plan', 'недостижимо · 3 хода')`, подпись под ходами демо 2, README | «недостижимо · 3 хода»; «Каждый ход уже посчитан — планер приносит клиенту выбор.»; «Три хода от дешёвого к дорогому» | «недостижимо · 3 варианта»; «Каждый вариант уже посчитан — выберите подходящий.»; «Три варианта…» | Второе значение «хода»; «планер» — планёр-самолёт; «приносит клиенту выбор» — калька |
| label `#auto_limit` + подсказка | «Лимит автоматики за один ход, ₽» / «переносы дороже этой суммы ждут вашего решения; пусто — автоматика решает сама» | «Лимит на один перенос, ₽» / «дороже — ждёт вашего решения; пусто — без лимита» | Подпись в две строки, подсказка не влезает в одну |
| labels `#mode`, `#target_kpi`, `#target_value` | «Постановка», «Цель в чём», «Целевой объём» | «Задача», «Что нужно», «Сколько нужно» | Рабочий жаргон, а не язык рекламодателя |
| подсказка `#horizon` | «в кейсе 14–21» | «обычно 14–21» | «Кейс» — слово жюри, не пользователя |
| `stage('brief', …)` | «1 200 000 ₽ · 21 день · 8 кан.» | «… · 8 каналов» через `pw(n, ['канал','канала','каналов'])` | Обрубок рядом с полными словами; на 1280 помещается |
| `renderPlan`, кнопка | «Что если бюджет −10 % / +10 %» | «Что если бюджет ±10 %» | Самая длинная кнопка в ряду; обычный пробел перед % |
| `whatIf`, подпись и заголовок столбца | «Отдача вогнутая: последние 10 % бюджета приносят меньше, чем первые. Это цифра для разговора о бюджете.»; th «Цена каждой добавленной или потерянной единицы» | «Последние 10 % бюджета приносят меньше, чем первые — с этой цифрой можно обсуждать бюджет.»; th `h2('Цена единицы', 'прироста / потери, ₽')` | «Отдача вогнутая» — термин из оптимизации; заголовок в две длинные строки |
| `renderPlan`, абзац под таблицей | «…канал остановлен перед изломом кривой из ретро-истории… * VTR — допущение 35 % без публичного источника (config/assumptions.yaml); досмотры симулятор не моделирует.» | Первое предложение оставить; второе и хвост про VTR завернуть в `<span class="tech">`; в «Кабинете» остаётся «* VTR — допущение 35 %.» | «Излом кривой», «ретро-история», путь к YAML, «симулятор» — не для рекламодателя |
| `renderPlan`, подпись календаря | «…внутри суток — по профилю часа из ретро-истории. Недельного профиля объёма нет ни в плане, ни в мире симулятора (README, раздел «упрощено»).» | «Бюджет разложен по дням равномерно, внутри дня — по часам активности.» + вторая фраза под `tech` | README и «мир симулятора» в «Кабинете» |
| `runCampaign` | «гоним кампанию по часам, две-три секунды…» | «считаем кампанию по часам, две-три секунды…» | Просторечие выбивается из ряда «считаем…» |
| `prepareRun`, плеер | «скорость [час / 6 часов / сутки] за тик» | «шаг: час / 6 часов / сутки» | «Тик» — жаргон |
| `prepareRun`, заголовки и легенды графиков | «Накопительно, конверсий: план, факт, план без изменений»; легенда «факт · план без изменений»; «доля расхода по каналам по дням, %» | «Конверсии накопительно: план, факт, план без изменений» (`cap(K.nomPl)`); легенда «план без изменений»; «доля каналов в расходе по дням, %» | Родительный в начале заголовка; «факт ·» перед «план без изменений» противоречит сам себе; двойное «по» |
| все `cap(K.many)` в заголовках плиток и таблиц (`showHour`, `renderSummary`) | «Конверсий накопительно», «Конверсий накопит.», «Конверсий по плану» / «Конверсий получено» | `cap(K.nomPl)`: «Конверсии накопительно», «Конверсии по плану»; второй столбец — просто «Получено» | Родительный без управляющего слова как заголовок |
| `showHour`, плитка двойника | «Без перераспределения было бы» / «тот же мир, план буквально (+12)»; при static: «Стратегия» / «план буквально» | «План без изменений дал бы» / «тот же рынок, без переносов (+12)»; при static: «Ведение» / «план без изменений» | Три названия одного и того же на одной плитке |
| `cardText` | «сделано автоматически: 86 702 ₽ в пределах вашего лимита 100 000 ₽» | «сделано автоматически: 86 702 ₽ ниже лимита 100 000 ₽» | Параллельно строке «выше лимита»; «в пределах вашего» — канцелярит |
| `cardText`, `feedLines`, `decideProposal` (отказ) | «вы оставили как есть: деньги остались в «SMS»»; «вы оставили как есть, предлагалось перенести X из … в …»; «Ход день 12, 06:00 оставлен как есть. Деньги остались в канале-доноре…» | «по вашему решению деньги остались в «SMS»»; «вы отклонили перенос X из … в …»; «Перенос (день 12, 06:00) отклонён. Деньги остались в прежнем канале; …» | «оставили… остались» — тавтология; «Ход день 12» без предлога не читается; «донор» — жаргон |
| `decideProposal` (одобрение) | «Ход день 12, 06:00 сделан. Цена решения по факту: +12 конверсий и +1 234 ₽ к итогу…» | «Перенос (день 12, 06:00) сделан. Что изменилось: +12 конверсий и +1 234 ₽ к итогу…» | «Цена решения по факту» — калька с cost of decision |
| `digest`, совет при «наблюдаем» | «вышли за коридор, но слома нет: следующий пересчёт не позже чем через 6 часов, ничего не делать» | «вышли за коридор, но канал цел: пересчёт не позже чем через 6 часов, делать ничего не нужно» | «Ничего не делать» звучит как команда; «слома нет» — жаргон |
| `digest`, `renderSummary` | «деньги остались у донора»; «100 000 ₽ не потрачены и возвращаются»; «Если бы план исполнялся без изменений, было бы…» | «деньги остались в прежнем канале»; «100 000 ₽ не потрачены — возвращаем»; «План без изменений дал бы…» | «Донор» — жаргон; пассив; длинное условное |
| `renderSummary`, `keptNote` | «−3,1 % внутри разброса ±34 %» | «−3,1 %, внутри разброса ±34 %» | Без запятой число сливается с «внутри» |
| `renderSummary`, плитка | «Ходов автоматики» / «обращений к человеку: 1» | «Переносов автоматики» / «ждали вашего решения: 1» | «Обращение к человеку» — голос системы, а не пользователя |
| NBSP в литералах: шапка («1,2 млн ₽»), labels «Бюджет, ₽», «Потолок CPA, ₽», «Лимит…, ₽», подсказки «0,6 = −40 %», вердикт демо 2, плитки, кнопка «±10 %», абзац про VTR «35 %», стресс-тест, th «Порог 20 %», заголовки сравнения и гистограммы; `main.py:67, 70` (титулы сценариев «CTR −40 %», «CR −40 %») | обычный пробел перед «₽» и «%» | ` ` в JS-строках, `&nbsp;` в HTML, ` ` в Python | Единственное место с NBSP в разметке — подсказка силы шока; остальные строки могут порваться перед знаком. Быстрый способ: в редакторе регуляркой `(\d) %` → `$1 %` и ` ₽` → ` ₽` только внутри строковых литералов и HTML-текста |

### Стенд

| Где | Сейчас | Предложение | Почему |
|---|---|---|---|
| `degradationTest`, h3 | «…в «SMS» с день 11, 00:00, мир 1» | «…в «SMS» со дня 11, мир 1» (`со дня ${Math.floor(startHour/24)+1}`) | `с ${hourStartLabel()}` даёт «с день» — ошибка падежа |
| `renderVerdict` | «Вердикт по кампании. с перераспределением: отклонение… — порог кейса 20 % выполнен.» | `cap(STRATEGY_RU[...])`; «порог кейса 20 % соблюдён / превышен» | Строчная после точки; порог не «выполняют» |
| `app/main.py` `STRATEGY_TITLES` + `compareStrategies` th, `stressTest`/`degradationTest` th, легенда гистограммы | «с перераспределением»; «без перераспределения (план буквально)»; «Побед над заморозкой»; «Без изменений:»; «наше ведение» | adaptive → «наше ведение», static → «план без изменений»; th `h2('Побед над планом', 'без изменений по ' + K.dat)`; th «План без изменений:» | Одна стратегия сейчас имеет пять имён (см. словарь). Тесты титулы стратегий не проверяют |
| `compareStrategies`, абзац | «…важна не только средняя, но и хвост за порогом…»; «с 95 % доверительным интервалом»; один абзац на 90 слов | «важны и средняя, и хвост за порогом»; «с 95-процентным доверительным интервалом»; разбить на два абзаца: что показано / как читать MAPE | Негативный параллелизм; «95 % доверительный» не согласуется; длина |
| `degradationTest`, подпись | «…при силе ×2: это граница применимости, названная явно.» | «…при силе ×2 — дальше решение порог не держит.» | «Названная явно» — самопохвала, читается угловато |
| подсказка `#strategy` | «базовые стратегии — для сравнения, не для показа» | «базовые — только для сравнения» | «Не для показа» двусмысленно |
| `histogram`, заголовок | «Сколько миров попало в каждую полосу отклонения по конверсиям в конце кампании, %» | Две строки массивом: `['Сколько миров в каждой полосе отклонения', 'по конверсиям в конце кампании, %']` | Не помещается в одну строку над графиком 230 px |
| `stressTest`, вердикт | «На двадцати мирах — кнопка «Сравнить стратегии» ниже.» | «На 20 мирах — «Сравнить стратегии на 20 мирах» в «Дополнительно» ниже.» | Кнопка называется иначе и спрятана в раскрывающемся блоке |

### Документы

| Где | Сейчас | Предложение | Почему |
|---|---|---|---|
| `README.md`, «Три демонстрации», демо 1 и 3 | Один абзац на 14 и на 22 строки, всё через точку с запятой | Разбить каждое демо на 3–4 предложения по экранам: бриф → плитки → таблица → кнопки; список плиток отдельной строкой | Коллега не найдёт нужную плитку в сплошном перечислении |
| `README.md`, демо 1 | «раскрывашка «как распределяли»» | «раскрывающийся блок «Как распределяли»» | Просторечие в документации; регистр как в интерфейсе |
| `README.md` («Результаты стенда», «Как устроено») + `docs/cabinet_changes.md` | «без перераспределения было бы», «план без изменений», «static (заморозка)», в таблице «заморозка» / «наша», «Заморозка тратит…», «двойник-заморозка», «для заморозки», «заморозка часто ближе» | Везде «план без изменений» (в таблице кратко «без изменений»), adaptive — «наше ведение»; «`static` (план без изменений)» | Единый термин с интерфейсом; «заморозка» нигде в UI не появляется |
| `docs/cabinet_changes.md`, «Круг третий», обещание | «(цель, увеличенная на ширину коридора, обещаем исходную)» | «бюджет считаем под цель, увеличенную на ширину коридора, а обещаем исходную» | Скобка без глагола не читается |

Проверено и менять не надо: «2 271 конверсия», «на двое/трое/четверо суток», «+ 1 свой шок / 2 своих шока / 5 своих шоков», «обещание выполнено» — согласование верное; кавычки «ёлочки», тире «—», многоточие одним символом; точек в подписях плиток и подсказок нет единообразно, в абзацах `.muted` есть — тоже единообразно.

### Словарь терминов

«План без изменений» (кратко «без изменений») — единственное имя стратегии `static` в «Кабинете», «Стенде», README и docs; «заморозка», «план буквально», «без перераспределения» уходят. «Наше ведение» — имя стратегии `adaptive` в «Стенде» и документах; в «Кабинете» отдельного имени нет, это просто «факт». «Перенос» — любое перемещение денег между каналами в «Кабинете» (лимит, карточки, лента, итог); «ход» допустим только в docs как синоним. «Вариант» — три предложения планировщика в демо 2 (Кабинет и README). «Карточка» — предложение переноса, ждущее решения (оба уровня). «Слом канала» — в «Кабинете»; «детектор» — в «Стенде» и документах. «Рынок» и «случай» — в «Кабинете» вместо «мир»; «мир», «зерно», «сценарий» — в «Стенде» и документах. «Разброс ±N %» — в «Кабинете»; «коридор ±1σ» — в «Стенде». «Обещание» без «клиенту» — в «Кабинете»; «обещание клиенту» — в README и docs. «Событие» — общее слово для ленты (шок рынка, свой шок, слом); «шок» — в «Стенде». «Накопительно» — единая форма в плитках и графиках, «накопит.» только в узких заголовках таблиц.

## Часть 2. Инженер: логика и контракт

Проверено: оба файла целиком, синтаксис JS, curl ко всем восьми эндпоинтам (демо 1, демо 2, «с запасом» в B, decide approve/decline, compare/stress/degradation с замером времени, граничные случаи), живая страница при 1280 и 1000 px (computed style, ширина документа, переключение уровня посреди прогона, решение по карточке).

### В каком порядке чинить (решение PM)

1. Инженер 1 — план приходит уже утверждённым. Три строки, и **перед показом перезапустить uvicorn**: без перезапуска первый же клик «демо 1» на сервере, где план утверждали на репетиции, покажет два «текущих» этапа.
2. Инженер 3 — сбросить этап «Итог» в `prepareRun`. Одна строка.
3. Инженер 2 — «с запасом» в постановке B: цель клиента в полосе этапов и вердикте, защита от недостижимого второго плана.
4. Инженер 4, 16, 14, 18 — карточка, ждущая решения, в конце; лишние 22 px у кнопок без подписей; техника, видимая в «Кабинете»; неразрывные пробелы.
5. Редактор: три ошибки падежей и словарь терминов (перенос, план без изменений, слом канала, рынок).
6. Всё остальное по мере сил.

### Сломано на демо

1. **`renderPlan` / `showRunSection` + `main.py` (`/api/plan`, `/api/plan/{id}/approve`) — план приходит уже «утверждённым», а полоса этапов говорит обратное.** `plan_id` детерминирован по брифу, сервер помнит утверждение, поэтому повторный `POST /api/plan` того же брифа возвращает `approved_version: 1` (проверено: свежий бриф → approve → тот же бриф → `approved_version=1`). `renderPlan` ставит этап 2 «прогноз … · не утверждён» `cur`, потом `showRunSection` ставит этап 3 `cur`: два текущих этапа, бейдж «утверждён, версия 1», кнопка «Утвердить план» на месте, раздел 3 открыт без шага утверждения. Как повторить: демо 1 → Утвердить → демо 1 ещё раз. Правка: в `renderPlan` `stage('plan', p.approved_version ? `утверждён · …` : `прогноз … · не утверждён`, p.approved_version ? 'done' : 'cur')` и прятать или отключать `#btn-approve` при `approved_version`.

2. **`makePlan` (двойной расчёт), `stage('brief')`, вердикт демо 2 — «с запасом» в постановке B подменяет цель клиента.** Второй план считается под цель/(1−corridor), и его `brief.target_value` попадает в этап 1: «75 825 кликов · 14 дней» при введённых 50 000 (проверено в браузере). Хуже: если второй план недостижим, вердикт пишет «меньше цели 54 023» с вариантами под цель, которую никто не задавал, а вариант «снизить цель до 35 451» снова раздувается → снова недостижимо (узкий пресет: 35 451 достижимо, 54 023 нет — зацикливание, план «с запасом» на узком наборе получить нельзя). Правка: если второй план `infeasibility` — показывать базовый план с пометкой «резерв недостижим, план медианный»; в `stage('brief')` и вердикте брать `PLAN._promise.value` (исходную цель), а не `b.target_value`.

3. **`prepareRun` — после решения по карточке этап 4 остаётся «текущим» со старыми цифрами.** `prepareRun` прячет `#summary`, но `stage('summary')` не сбрасывает: остаётся `cur` «обещание выполнено · 2 181 конверсия» из прошлого прогона, клик по этапу ничего не делает (проверено: К итогу → час 12 → Перенести). То же при повторном «Запустить кампанию». Правка: в `prepareRun` добавить `stage('summary', 'в конце кампании', 'off')`.

### Ошибки логики

4. **`showHour`, карточки — в конце кампании карточка, ждущая решения, не видна.** Видны последние 4 из 9 переносов; ждущий час 12 за бортом. Сводка пишет «Не решено вовремя: 1 карточка», действовать негде. Правка: всегда добавлять ждущие карточки первыми: `[...pending, ...cards.slice(-4)]` с дедупликацией.

5. **`whatIf` / `stressTest` не проверяют `PLAN !== plan` после `await`** и пишут в `#whatif` / `#stress` того плана, что отрисован сейчас → результат старого плана под новым. Как повторить: «Что если» → сразу демо 2. Правка: `const plan = PLAN; … if (PLAN !== plan) return;`.

6. **`compareStrategies` / `degradationTest` вставляют `box` в `#run-body`, а `runCampaign` / `decideProposal` заменяют `#run-body.innerHTML`** → box отрывается, результат минуты счёта теряется молча, кнопка разблокируется. Правка: отключать `#btn-run` на время стендов или проверять `box.isConnected` и выводить сообщение.

7. **Кнопки-примеры не блокируются во время `makePlan`.** Двойной клик демо 1 / демо 2 → два параллельных расчёта, побеждает последний ответ, он может не совпадать с формой (форма в B, план A). Правка: общий флаг `busy` или отключать оба `#btn-demo*` вместе с `#btn-plan`.

8. **`makePlan` — коридор берётся у первого плана**, у второго он другой (0,3406 → 0,3377), поэтому `p10 ≥ цель` не гарантировано, а плитка утверждает «цель внутри разброса даже по нижней границе». Правка: после второго плана проверять `plan.forecast.p10 >= target` и формулировать плитку условно.

9. **`renderSummary` — «с запасом» в B: «обещание выполнено» меряется от 50 000, а «Отклонение по кликам» и «Порог кейса» — от `promised_kpi` = 75 838.** Рекламодатель видит «выполнено» рядом с большим отклонением. Правка: подпись плиток «от плана с резервом» либо считать отклонение от обещания.

10. **`renderScenarioOpts` / `updateRunCaption` — при сроке меньше 11 дней все сценарии `na` (события с 240 ч), но `FORM.scenario` остаётся `channel_pause`, и в «Кабинете» (где карточки скрыты) подпись «на рынке: programmatic выключен на трое суток» врёт.** Правка: в `updateRunCaption` добавлять «— за горизонтом, не случится» или автоматически переключать на `stable`.

11. **`degradationTest` с параметром `pause` перебирает множители 0,8…0,2, что для паузы бессмысленно;** в таблице `×${x.multiplier}` печатается с точкой («×1.5»). Правка: для `pause` не запускать (или множители `[1]`), `String(x.multiplier).replace('.', ',')`.

12. **`addShock` пропускает дубли (тот же канал/параметр/день) и шоки по каналам с нулевым бюджетом** (`allocations` содержит все 8 каналов даже при `is_empty`, `_check_shock_target` тоже смотрит на allocations). Мелочь.

13. **`histogram` при пустом `per_run` молча выходит,** остаётся пустой canvas без подписи. Правка: писать «нет данных» в контейнер.

### Кабинет против Стенда

14. **Техника видна в «Кабинете»:** «(config/assumptions.yaml); досмотры симулятор не моделирует», «в мире симулятора (README, раздел „упрощено“)», «одна σ из ширины диапазонов каталога» (проверено: `techVisible: 0`, а текст на экране). Правка: обернуть эти фразы в `<span class="tech">` или переформулировать (совпадает с редактором).

15. **`hold_plan` («вернуть неизрасходованное / потратить весь бюджет») — деловой выбор рекламодателя, а спрятан в раскрывающемся блоке `.tech`;** от него зависит плитка «Возвращено бюджета». Предложение: вынести из `.tech` (например, в «Дополнительно» брифа).

### Вёрстка

16. **`.row > button{margin-top:22px}` бьёт по строкам без подписей:** `#btn-run`, «Утвердить план» / «Что если» / «Стресс-тест», кнопки карточки «Перенести» / «Оставить» — 22 px пустоты сверху, `#run-caption` ещё 29 px (проверено computed style). Правка: `.row:has(label) > button{margin-top:22px}` или отдельный класс для строк с полями.

17. **`fixHeaders` ищет только `span.w`,** поэтому заголовки из `h2()` (`span.w2`) длиннее 11 символов оборачиваются ещё и в `span.w`. Сейчас безвредно, но двойная обёртка. Правка: `th.querySelector('span.w,span.w2')`.

### Типографика

18. **NBSP в файле только в `pct` / `rub` и один `&nbsp;`. Обычный пробел перед ₽/% примерно в 25 строковых литералах** `index.html` и в `main.py` (титулы сценариев «CTR −40 %», «CR −40 %» и `_rub` — уходит в ошибки API: «зафиксировано 0 ₽»). Правка: заменить на неразрывный или использовать `rub()` / `pct()` (совпадает с редактором).

19. **`fmt` / `pct` для отрицательных чисел дают дефис-минус:** баннер «-339 ₽», Δ в «Что если», Δ по каналам в итоге. Правка: `.replace('-', '−')` в `fmt` и `pct`.

20. **Для `max_reach` плитка «цена за одного 0 ₽»** (0,38 ₽ при `fmt(unit)` без дробной части; проверено curl). Правка: `fmt(unit, unit < 10 ? 2 : 0)`.

21. **«1 496 – 3 047»:** диапазон с отбитым коротким тире; по-русски «1 496–3 047» или «от … до …».

22. **`hourLabel` даёт «день 1, 24:00» в ленте/хронологии/сводке;** а в сводке на последнем часе «Что делать: кампания идёт по плану, ничего делать не нужно» рядом с «Не решено вовремя: 1 карточка». Правка: при `H === N` писать «кампания завершена».

### Проверено и в порядке

Синтаксис JS; все имена полей, которые читает фронт, есть в ответах API (`/api/plan` — `forecast.p10/p50/p90`, `corridor_rel`, `is_empty`, `infeasibility.suggestions[].changed_field/suggested_value/applicable/why_not/binding_title`, `allocations[].marginal_cost_per_1000_kpi_rub`; `/api/run` — `main.hours[].by_channel/caps/reserve/events/status`, `proposals[].cpa_delta_pct/inaction_kpi_shortfall_pct/applied_by`, `frozen.hours`, `verdict.*`, `custom_shocks`, `seeds`, `decisions`; `decide` — `effect.kpi_delta/spend_delta/deviation_kpi_before/after`; `compare` — `mean/ci95/win_rate_vs_static/mean_actual_kpi/mean_actual_spend/per_run/within_threshold_count/title`; `stress` / `degradation` rows; `meta.catalog.channels[].sms.send_hours`). Согласованность часов: `shock_hours` = 0-based `start_hour` → запись +1 → `hourStartLabel`; `detection_hours` / `proposals[].hour` 1-based → `hourLabel`; `eventLine` «с дня 11» = `shockFromForm` (day−1)·24 = 240 = сценарные события. Ширина документа равна окну при 1280 и 1000 px (за край выходят только таблицы внутри `.scroll`). Переключение уровня посреди прогона: canvas подстраивается под родителя, `.two` меняет 2→1 колонки, выбор пишется в `localStorage`. Инпуты, селекты и кнопки 36 px, чипы 30, селект плеера 30. Ошибки бэкенда по-русски (пустой набор каналов, шок вне плана или за горизонтом, больше трёх шоков, неизвестный сценарий, approve пустого или недостижимого плана → 409, decline после approve → 422). `compare` на 2 мирах 7,8 с → на 20 около 80 с, в таймаут 180 с укладывается; `stress` 19 с, `degradation` 12 с. Описания демо в README совпадают с поведением.
